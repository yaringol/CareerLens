# Copy to .env (gitignored) for `docker compose up`. Referenced by docker-compose.yaml.
MONGO_ROOT_PASSWORD=changeme
JWT_SECRET=dev-insecure-change-me
OPENAI_API_KEY=
# Pipeline / model training DB (separate from careerlens app DB)
JOBS_MONGO_URI=mongodb://localhost:27017/jobs
