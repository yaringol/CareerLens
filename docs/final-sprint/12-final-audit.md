# משימה 12: ביקורת סיום - סגירת כל הקצוות הפתוחים

> בריפינג לאייג'נט עצמאי. רץ **אחרון**, אחרי שכל שאר המשימות סומנו ☑ במסמך המאסטר.
> זו דרישה מפורשת של המשתמש: "לוודא שאחרי כל התכנון כל הקצוות הפתוחים סגורים ואין עוד בעיה."
> כללי הברזל מ-[00-MASTER-PLAN.md](00-MASTER-PLAN.md) מחייבים.

## מטרה

מעבר ביקורת עצמאי, חשדני ושיטתי על כל תוצרי הספרינט - לא לסמוך על הדיווח של אף משימה,
אלא **לאמת מחדש כל DOD בפועל**. התוצר: דוח ביקורת עם pass/fail לכל סעיף. כל fail פותח
מחדש את המשימה הרלוונטית, והביקורת חוזרת עד אפס ממצאים.

## עיקרון

אתה לא מתקן (מלבד זוטות של דקה) - אתה מאתר, מתעד, ומדווח. תיקונים חוזרים למשימת המקור
כדי שהאחריות לא תתמרח.

## הצ'קליסט

### א. קוד ו-git
- [ ] `git status` נקי; אין קונפליקטים; הכל דחוף ל-remote.
- [ ] אף קובץ `.env*` לא נכנס ל-git מעולם בספרינט הזה (`git log --diff-filter=A --name-only`
      על התקופה + בדיקת tracked files); אין סודות בקוד (grep ל-api keys/passwords/tokens).
- [ ] אין זכר ל-AI באף הודעת קומיט מהספרינט (`git log --format=%B` + grep).
- [ ] `npx tsc --noEmit` ו-builds עוברים ב-backend וב-frontend; DS server עולה נקי.
- [ ] grep-ים של משימה 02 עדיין נקיים (console.log, TODO, אימוג'ים) - שמשימות מאוחרות לא החזירו לכלוך.
- [ ] קבצים גדולים/בינאריים לא שייכים נמצאים מחוץ ל-git (בדוק gitignore מול המצב בפועל).

### ב. מערכת חיה (עם Playwright MCP - חובה, לא להסתפק ב-API)
- [ ] הזרימה המלאה עובדת: upload → auto-detect → personalize → analyze → results →
      improve → save plan → account history. על 2 קו"ח שונים מהסט של משימה 04.
- [ ] בדיקת 59 הטייטלים: אף title בלי סקילז ב-`model.joblib` המוגש (הבאג ההיסטורי!).
- [ ] מסך Personalize לא מציג role-mismatch (הבאג ההיסטורי השני).
- [ ] אפס שגיאות console בדפדפן ואפס unhandled errors בלוג ה-backend לאורך הזרימה.
- [ ] הטקסטים במסכים תואמים את מה שמשימה 03 דיווחה (בדיקה מדגמית של 5 מסכים).

### ג. תוצרי המשימות (אימות קיום + איכות מדגמית)
- [ ] `test-fixtures/authentic-cvs/`: manifest תקין, מספר קבצים כמוצהר, PDF נפתחים.
- [ ] `outputs/model2-benchmark.md`: קיים, המספרים בו עקביים עם קבצי ה-raw, והסף בקוד
      (`AUTO_MATCH_CONFIDENCE_MIN`) תואם את ההמלצה הסופית בדוח.
- [ ] דוח הדאטה (`outputs/06-data-analysis.md`) קיים; ריצת promote אחרונה ב-`model_runs`
      מוצלחת; אם היה backfill סינתטי - מתועד.
- [ ] טבלת משימה 07: לכל פריט החלטה + סטטוס; אין פיצ'ר חצי-עשוי גלוי ב-UI.
- [ ] חומרי שיווק: סקרינשוטים בלי שגיאות טקסט (זום!), flyer-content, video-script - קיימים ואושרו.
- [ ] ספר הפרויקט: כל סעיפי התבנית מלאים; **בדיקת עקביות צולבת** - כל מספר בספר (accuracy,
      counts, מספרי titles) תואם את מקורו בדוחות; כל figure קיים; כל רפרנס אמיתי (בדוק מדגמית 5).
- [ ] כרוניקת הניסוי-וטעייה: הראיות (קומיטים/מסמכים) שהיא מצטטת קיימות (בדוק מדגמית 5).
- [ ] runbook הדמו: להריץ את פקודת ההרמה פעם אחת מאפס ולוודא שהיא באמת עובדת כמתועד.

### ד. עקביות רוחבית (הקצוות שנופלים בין משימות)
- [ ] אין סתירות בין הספר ↔ README ↔ הקוד (למשל: מספר תפקידים נתמכים, רשימת פיצ'רים,
      הוראות setup).
- [ ] `docs/ds-models/INDEX.md` ושאר מסמכי התיעוד לא מפנים לקבצים שנמחקו/הועברו בספרינט.
- [ ] כל השאלות הפתוחות במסמך המאסטר (סעיף אחרון) קיבלו מענה - אף ❓ לא נשאר תלוי.
- [ ] סטטוס כל המשימות במסמך המאסטר מעודכן ל-✔ אחרי המעבר.
- [ ] חיפוש חופשי אחרון: `grep -ri "placeholder\|lorem\|xxx\|tbd\|החלף\|לבדוק" ` על docs
      החדשים והספר - אפס ממצאים לא-מכוונים.

## תוצר

`docs/final-sprint/outputs/final-audit-report.md`:
- טבלת pass/fail לכל סעיף בצ'קליסט + ראיה (פלט פקודה/צילום מסך/קישור).
- רשימת ממצאים ממוינת לפי חומרה, כל אחד משויך למשימת המקור שנפתחת מחדש.
- בסבב שבו הכל עובר: הצהרת סיום + עדכון כל הסטטוסים במסמך המאסטר ל-✔.

## הגדרת Done

- [ ] כל סעיפי הצ'קליסט PASS (אחרי כמה סבבים שצריך).
- [ ] דוח הביקורת מלא עם ראיות.
- [ ] המשתמש קיבל סיכום סופי: מה נבדק, מה נמצא ותוקן בדרך, והצהרה שאין קצוות פתוחים.

## גבולות

- לא לתקן ממצאים לא-טריוויאליים בעצמך - להחזיר למשימת המקור (אייג'נט חדש עם הבריפינג שלה + הממצא).
- אם מתגלה בעיה שאין לה משימת-אם (קצה שנפל בין הכיסאות) - לדווח למשתמש ולהציע איך לטפל,
  לא להחליט לבד.
